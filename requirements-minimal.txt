# Minimal dependencies for VideoMind discovery and LLM features
# Install with: py -m pip install -r requirements-minimal.txt

# Core HTTP and API
requests>=2.31.0
tqdm>=4.65.0

# LLM Integration
langchain>=0.1.0
langchain-ollama>=0.1.0
ollama>=0.1.0

# FastAPI (optional, for REST endpoint)
fastapi>=0.100.0
uvicorn>=0.24.0

# Utils
python-dotenv>=1.0.0
yt-dlp>=2023.12.0

# In-memory ingestion
decord
faster-whisper
opencv-contrib-python-headless>=4.8
